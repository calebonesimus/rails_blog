

<div class="container">
  <div class="row">
    <% @posts.each do |post| %>
      <% if post.title.downcase.include?(params[:search]) %>
        <h2><%= link_to post.title, post_path(:id => post.id) %></h2>
        <p><%= post.summary %></p>
      <% end %>
    <% end %>
  </div>
</div>
